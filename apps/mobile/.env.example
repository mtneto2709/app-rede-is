# Ver descrição completa em /ENVIRONMENT.md na raiz do repo.
# Definidas por cliente em clients/<slug>/.env e injetadas via EAS Build
# (ver eas.json) — nunca alteradas em runtime dentro do app.
EXPO_PUBLIC_TENANT_SLUG=demo
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
